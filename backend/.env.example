# Backend Environment Variables
# Neon Serverless Postgres Connection String
# Use the pooled connection string (-pooler) for serverless environments
DATABASE_URL="postgresql://user:password@ep-cold-shadow-123456-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require"

# Shared secret for Better Auth JWT Verification (MUST MATCH FRONTEND)
BETTER_AUTH_SECRET="super-secret-key-change-in-production"

# Optional: CORS origins
ALLOWED_ORIGINS="http://localhost:3000"
